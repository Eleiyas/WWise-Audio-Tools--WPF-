<UserControl x:Class="WWiseToolsWPF.Views.Help"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
			<!--<StackPanel.Background>
				<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0" SpreadMethod="Pad">
					<GradientStop Color="#FF3D3D3D"/>
					<GradientStop Color="White" Offset="1"/>
				</LinearGradientBrush>
			</StackPanel.Background>-->
            <TextBlock Text="Help" FontSize="26" FontWeight="SemiBold" Foreground="Black" HorizontalAlignment="Center" Margin="0,0,0,12"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto"
              Padding="10">

                <TextBlock TextWrapping="Wrap"
               FontFamily="Segoe UI"
               FontSize="12"
               Foreground="DimGray">

        <Run Text="&#8226; " />
            <Run Text="Audio Extractor" FontWeight="Bold"/>
        <LineBreak/>
        <Run Text="Extraction tool for WWise audio packages (.pck) and banks (.bnk). Also supports (.chk), thanks to Escartem."/>
        <LineBreak/>
        <Run Text="Can output to WEM, split WEM, WAV or OGG."/>      
        <LineBreak/>
        <LineBreak/>
        <Run Text="Select input files of type '.pck', '.bnk', or '.chk', choose an output directory and whether or not you wish to use a generated mapping file."/> 
        <LineBreak/>
        <Run Text="Output format and settings depend on use-case:"/>            
        <LineBreak/> 
        <Run Text="Example: Selecting 'WEM' and 'Split Output' will extract the WEM files into folders corresponding to the name of the input file."/>   
        <LineBreak/> 
        <LineBreak/>             

        <Run Text="&#8226; " />
            <Run Text="FNV Hasher" FontWeight="Bold"/>
        <LineBreak/>
        <Run Text="A small program used to manually generate FNV hashes by inputting suspected filenames."/>
        <LineBreak/>
        <LineBreak/>            
        <Run Text="Depending on the game, filenames can either be found in ExcelData, BinData, or scraped from MonoBehaviours."/>            
        <LineBreak/>
        <Run Text="However, this program is more for testing and brute-forcing missing names, based on known data."/>            
        <LineBreak/> 
        <Run Text="Please ask Eleiyas via Discord for more details if needed."/>            
        <LineBreak/>
        <LineBreak/>            
        <Run Text="&#8226; " />
            <Run Text="Voice Items Collator" FontWeight="Bold"/>
        <LineBreak/>
        <Run Text="This program collates the filenames within the 'VoiceItems' folder of a specific Anime Game and saves them to a file."/>
         <LineBreak/>
        <Run Text="The file generated can be used directly with the 'FNV FileList Generator' to find 'NO MATCH' files for ease of use."/>            
        <LineBreak/>
        <LineBreak/>
        <Run Text="Select a language prefix, a proper Input folder (usually 'BinData/Voice/Items'), an Output folder, then run."/>             
        <LineBreak/>
        <LineBreak/>            

        <Run Text="&#8226; " />
            <Run Text="File-List Hasher" FontWeight="Bold"/>
        <LineBreak/>
        <Run Text="Small program using the same technique as the 'FNV Hash Generator', used to parse a large list of filenames and output to a text file."/>
        <LineBreak/>
        <LineBreak/>
        <Run Text="Select a valid list of filenames as Input, then run. Logger-box will only show unknown matches, to save on threading."/>            
        <LineBreak/>
        <LineBreak/>            

        <Run Text="&#8226; " />
            <Run Text="Downloads" FontWeight="Bold"/>
        <LineBreak/>
        <Run Text="Downloads needed libraries for the Audio Extractor to run. Namely 'VGMStream' and 'FFmpeg'."/>

                    <TextBlock.Resources>
                        <Style TargetType="Hyperlink">
                            <Setter Property="Foreground" Value="Goldenrod"/>
                            <Setter Property="TextDecorations" Value="{x:Null}"/>
                        </Style>
                    </TextBlock.Resources>
                </TextBlock>
            </ScrollViewer>
        
        </StackPanel>
	</Grid>
</UserControl>